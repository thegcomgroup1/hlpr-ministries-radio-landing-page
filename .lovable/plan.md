
# Website Care Plans — PDF + LP Section

Two deliverables, one source of truth so numbers always match.

## 1. Care Plans PDF (`/public/hlpr-care-plans.pdf`)

Single 8.5×11" print-ready page, generated by a Python script (`scripts/build-care-plans-pdf.py`) using ReportLab/Playwright HTML→PDF. Output written to **`public/hlpr-care-plans.pdf`** so the LP can link to it, and also copied to `/mnt/documents/` as a downloadable artifact.

Content (exact copy from your prompt):

- **Header band** (navy #1B2A4A): "Website Care Plans" + subtitle "Every hlpr Ministries website includes a monthly care plan — hosting, security, and a team that keeps it current."
- **3 columns**, Growth elevated (blue border, "Most Popular" badge, slightly larger card):
  - **Foundation — $49.99/mo** · "The essentials." → secure hosting & SSL, automatic backups, uptime monitoring, unlimited content updates within 24 hrs (text/email/Slack).
  - **Growth — $249.99/mo** · "Most popular." → "Everything in Foundation, plus:" **8 branded social graphics/mo** (2/week), profile setup & upkeep, monthly content guidance, priority support.
  - **Established — $449.99/mo** · "Maximum reach." → "Everything in Growth, plus:" weekly sermon publishing to YouTube, channel optimization & thumbnails, content strategy for reach, dedicated team member. *(No vidIQ mentioned.)*
- Full-width italic: "Plans are month to month. Upgrade or downgrade any time as your ministry grows."
- **Footer band** (navy): "hlpr Ministries · Named for the Holy Spirit — our Helper. · tim@hlpr.io"
- Playfair Display headings, clean sans body, #EEF3FA card backgrounds, #2E5FA3 accents. No lorem, no stock photos.

QA: render to JPG at 150dpi, inspect for overflow / clipped text / one-page fit.

## 2. Landing-page "Simple, Honest Pricing" section

New component `src/components/landing/CarePlans.tsx`, inserted in `src/pages/Index.tsx` **directly below `<FinalCtaBand />`** (the existing free-preview CTA band), above the FAQ. Existing `<Pricing />` section stays untouched per your answer.

Section structure (uses existing design system — Fraunces serif, brand-navy, brand-amber CTA, brand-cream bg, shadow-soft/elevated, same CtaButton):

- **Eyebrow + heading** (left-aligned): "Simple, Honest Pricing" / "One build price. One simple care plan. No surprises."
- **Build banner** (horizontal card): "$1,500 one-time website build" · "Split payment available" · brief line about what's included.
- **3 care-plan columns** beneath, same content as PDF, Growth elevated with "Most Popular" badge — matches `Pricing.tsx` visual pattern for consistency.
- Footnote: "Plans are month to month. Upgrade or downgrade anytime."
- **Download link**: subtle text link "Download the one-page Care Plans PDF →" pointing to `/hlpr-care-plans.pdf` (opens in new tab).
- **Repeat the free-preview CTA button** at the end (same `CtaButton` w/ `BUSINESS.ctaUrl` / `ctaLabel`).

Add tier data to `src/lib/content.ts` as `CARE_PLANS` array so PDF script and LP component can both consume it (script reads via a small JSON mirror or hard-coded matching values — copy is short enough to duplicate safely; I'll keep them in sync in the same change).

## Technical notes

- PDF generator: HTML/CSS template + headless Chromium (same pipeline used for v5 flyer) — gives us Playfair Display via Google Fonts and pixel control.
- Script lives at `scripts/build-care-plans-pdf.py`; output committed to `public/hlpr-care-plans.pdf` so Vite serves it at `/hlpr-care-plans.pdf`.
- LP changes are presentation-only — no business logic, no schema, no new routes.
- Nav: existing "Packages" link still points to existing Pricing; not changing nav.

## Out of scope

- No changes to existing `Pricing.tsx` ($1,500 build / 199 etc.) — left as-is per your choice.
- No vidIQ anywhere in client-facing copy.
- No new analytics events beyond what the repeated CTA button already fires.
